<!doctype html>
<html lang="id">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Lupa kata sandi — Laporan Keuangan</title>@vite(['resources/css/app.css'])</head>
<body class="login-page"><main class="login-shell"><div class="login-brand"><p class="eyebrow light">Pemulihan akun</p><h1>Laporan Keuangan</h1><p>Tautan reset dikirim melalui kanal email yang dikonfigurasi administrator.</p></div><section class="surface login-panel"><h2>Lupa kata sandi</h2><p class="help-text">Masukkan email akun. Demi keamanan, respons tidak mengungkapkan apakah email terdaftar.</p>@if(session('status'))<div class="notice notice-success">{{ session('status') }}</div>@endif @if($errors->any())<div class="notice notice-danger">{{ $errors->first() }}</div>@endif<form method="post" action="{{ route('password.email') }}">@csrf<label class="field-label" for="email">Email</label><input class="form-control" id="email" type="email" name="email" value="{{ old('email') }}" autocomplete="email" required autofocus><div class="form-actions"><button class="button button-primary" type="submit">Kirim tautan reset</button></div></form><p><a href="{{ route('login') }}">Kembali ke halaman masuk</a></p></section></main></body></html>
